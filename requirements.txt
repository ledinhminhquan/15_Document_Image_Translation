# Full local/dev install (everything). For a minimal core install use `pip install -e .`
# and add extras: `.[ml]`, `.[ocr]`, `.[api]`, `.[report]`, or `.[all]`.

# -- core (package + dictionary MT + SeedEngine offline OCR + fit-estimate + metrics + tests) --
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
Pillow>=9.5

# -- trainable MT core --
torch>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.1.99
sacrebleu>=2.3

# -- OCR front-end (Tesseract binary must be installed separately: apt-get install tesseract-ocr) --
pytesseract>=0.3.10
PyMuPDF>=1.23
# easyocr>=1.7            # optional alternative OCR engine

# -- serving (API + UI) --
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
python-multipart>=0.0.9

# -- reporting (auto report.pdf + slides.pptx) --
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7

# -- optional --
# anthropic>=0.39          # optional LLM terminology-note brain for the agent
