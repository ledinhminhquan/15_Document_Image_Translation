# Colab-safe dependencies. IMPORTANT: do NOT list torch here — Colab ships a CUDA
# build already, and reinstalling it breaks the runtime. The notebook installs these,
# then `pip install -e . --no-deps`. Tesseract is installed via apt in the notebook.

transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.1.99
sacrebleu>=2.3

pytesseract>=0.3.10
PyMuPDF>=1.23
Pillow>=9.5

fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
python-multipart>=0.0.9

reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7
